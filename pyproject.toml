[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shwap"
version = "0.0.1"
description = "Inventory-first sharing app for Frappe"
authors = [{ name = "shwap.trade" }]
dependencies = []
